# 2026-09-02 작업 내용 — 깊이 노이즈 대응

D435 raw depth의 노이즈를 줄이기 위해 두 가지를 넣었다. 하나는 **신뢰 없는 측정을 버리는 것**(게이트), 다른 하나는 **측정 불확실성에 맞춰 융합 폭을 좁히는 것**(적응형 트런케이션 밴드)이다. 셋 다 기본 OFF이며, `capture/` 재생이 변경 전과 bit-identical임을 확인했다.

## 1. 깊이 신뢰도 게이트

D400은 per-pixel confidence 채널을 내지 않는다(그건 L515다). 그래서 신뢰도를 케이블 양쪽에서 각각 만들었다.

### 호스트 측 — `DepthFilterOptions` ([DepthCameraFrameSource.h](../../src/Pipeline/Reconstruction/DepthCameraFrameSource.h))

| 노브 | 잡는 것 |
|---|---|
| `minimumDepthMeters` / `maximumDepthMeters` | 센서가 측정할 수 없는 거리. 범위 밖은 *unemitted*가 아니라 **invalid**로 만든다 — 안 그러면 옆 픽셀의 법선을 여전히 결정한다 |
| `minimumValidNeighbours` | 매처가 버린 픽셀 바다에 뜬 작은 섬. 기존 depth-jump 가드는 이웃 2개만 보고, 섬 내부는 그 둘과 일치하므로 못 잡는다 |
| `maximumIncidenceDegrees` | 스치는 입사각. 표면이 연속이라 어떤 불연속 가드로도 안 보인다 |

이웃 지지도는 **유효성이 아니라 "같은 표면"** 을 센다. 스텝 건너편 이웃은 다른 표면이고, 그걸 세면 이 게이트가 잡으려는 flying pixel을 그대로 통과시킨다.

### 디바이스 측 — `RealSenseOptions` ([RealSenseDepthProvider.h](../../src/Pipeline/Reconstruction/RealSenseDepthProvider.h))

High Accuracy 비주얼 프리셋, `rs2::threshold_filter` 범위, advanced-mode JSON. 매처가 raw 스테레오 쌍을 보고 내리는 판단이라 **케이블 이쪽에서는 얻을 수 없는 정보**다.

- **프리셋은 `get_depth_scale()` 전에 적용한다.** 비주얼 프리셋이 `RS2_OPTION_DEPTH_UNITS`를 바꿀 수 있어, 먼저 읽으면 이전 설정의 스케일이 잡히고 재구성 전체가 증상 없이 오배율된다.
- `RealSenseOptions`는 `VKBVH_HAS_REALSENSE` 가드 **밖**에 있다. rs2 타입을 하나도 안 쓰므로, SDK 없는 머신에서도 호출자가 만들 수 있다.

### 관측

`DepthFilterStats`가 원인별로 센다. **합치면 안 된다** — range는 픽셀을, 나머지 둘은 후보 점을 세는 다른 모집단이다.

## 2. 적응형 트런케이션 밴드

축방향 깊이 노이즈는 거리에 대해 **2차로** 자란다(Nguyen·Izadi·Lovell, 3DIMPVT 2012, eq. 4). 고정 트런케이션 하나는 근거리에서 과하게 넓고(밀리미터로 분해된 표면을 밴드 전체에 뭉갠다) 원거리에서 겨우 충분하다.

$$\sigma_z(z,\theta) = 0.0012 + 0.0019(z-0.4)^2 + \frac{0.0001}{\sqrt{z}}\cdot\frac{\theta^2}{(\pi/2-\theta)^2}$$

### 설계상 결정 — 정규화는 전역을 유지한다

**밴드 폭만** 바꾸고, 저장되는 tsdf는 빌드 시점 truncation으로 계속 정규화한다. `GpuIcpTracker`, `PointToPlaneIcpTracker`, `GlobalRegistrationTracker`, extract 커널이 전부 `tsdf * truncationDistance`로 미터 거리를 복원하기 때문이다. 점마다 정규화가 다르면 이들이 **표면을 엉뚱한 곳에 놓고 다른 증상은 없다.**

따라서 밴드는 **좁아지기만 한다.** 원거리를 넓히려면 빌드 트런케이션을 올리면 되고, 근거리가 더는 그 대가를 치르지 않으므로 이제 안전하다.

**하한(2 복셀)은 선택이 아니다.** 근거리에서 3σ는 몇 밀리미터로 10 mm 복셀보다 작고, 격자보다 좁은 밴드는 아무 샘플도 못 받아 **표면이 선명해지는 대신 사라진다.**

### 계수에 대한 유보

논문 계수는 Kinect v1(구조광) 피팅이다. D435는 액티브 스테레오라 **함수 형태는 전이되지만 상수는 아니다.** 그래서 노출해 두었고, 실제 카메라로 재피팅해야 한다.

## 3. 측정 (`capture/` 477프레임, 결정론적 재생 — 베이스라인 bit-identical 재현 확인)

### 게이트 (icp 트래커, prefilter 3)

| 게이트 | 제거 | 맵 복셀 | 궤적 |
|---|---|---|---|
| 없음 | – | 2,413,947 | 1.236 |
| range 0.3–4.0 m | 2.13M px (1.7%) | 2,410,337 (−0.1%) | 1.241 |
| support ≥6 | 1.25M (1.0%) | 2,404,970 (−0.4%) | 1.238 |
| incidence ≤75° | 2.50M (2.0%) | **2,772,065 (+14.8%)** | 1.268 |

### 적응형 밴드

| 설정 | 맵 복셀 | align ms | 궤적 | step max |
|---|---|---|---|---|
| 고정 밴드 | 2,413,947 | 161.40 | 1.236 | 0.0098 |
| **3σ_z 적응** | **1,875,783 (−22.3%)** | **126.65 (−21.5%)** | 1.203 | 0.0086 |

추적은 모든 설정에서 **476/476**, 거부는 프레임 0의 `NoModel` 1건뿐.

## 4. 미해결

- **incidence 게이트의 비단조성.** 점 2% 제거에 맵 15% 팽창. 포즈 고정(`identity`)에서도 +8.7%라 포즈 효과가 아니고, 버퍼는 클램프가 아니라 성장하므로 용량 천장도 아니다. 85°(−0.004%)와 60°(−16.5%)는 단조로운데 75°만 어긋난다. **원인 미규명.** 맵이 directional TSDF(`maxDirections` 3)라 entry 수가 표면 넓이와 1:1이 아닌 것이 유력하지만 검증하지 않았다.
  - 참고: [`MapConfig::viewAngleWeight`](../../src/Pipeline/Types.h)가 이미 `true`라 융합은 이미 cos(θ)로 가중되고 있었다. hard 게이트는 그 위에 이중으로 얹힌 셈이다. **끄고 쓰는 것을 권한다.**
- **적응형 밴드의 GT 검증 불가.** 유일한 미터 depth 데이터인 `capture/`에 ground truth가 없고, `scanData`/`scan_out`은 밀리미터 스케일이라 이 모델이 사실상 무효다(7031→7029). 정확도는 합성 평면 단위 테스트로만 확인했다.
- σ_z 계수 D435 재피팅.
- 다음 우선순위(어제 정리한 것 중 2~5): σ 가중 prefilter, ICP 잔차 가중, High Accuracy 프리셋 실측, `viewAngleWeight`를 cos에서 σ 기반으로.

## 5. 테스트

전체 **291 통과**. `DepthFrontend` 32개(+14), `AdvancedTSDF` 13개(+6). 전부 TDD로 RED를 먼저 확인했고, 판정에 관여하는 단언은 뮤테이션으로 검증했다:

| 뮤테이션 | 잡은 테스트 |
|---|---|
| 게이트 기본값 3개를 켬 | `TheConfidenceGatesAreAllOffByDefault` (단언 4개 개별 실패) |
| 이웃을 유효성만으로 셈 | `NeighbourSupportCountsOnlyNeighboursOnTheSameSurface` |
| 입사각을 `n.z()`로 잼 | `IncidenceIsMeasuredAgainstTheViewRayNotTheOpticalAxis` |
| 밴드 상한 클램프 제거 | `TheAdaptiveBandKeepsTheFullTruncationFarAway` |
| 밴드 하한 제거 | `TheAdaptiveBandNarrowsANearSurface`, `...FloorKeepsTheSurfaceAtCloseRange` |
| 점별 정규화로 변경 | `EveryBandEntryDenormalizesWithTheBuildTimeTruncation` |

마지막 항목은 처음에 `ExtractPointCloud`로 재려다 **뮤테이션을 못 잡아** 고쳐 쓴 것이다 — 영교차는 tsdf를 양수배해도 안 움직이므로 추출은 정규화에 둔감하다. 민감한 쪽은 모든 엔트리를 복원하는 트래커다.

## 6. 변경 파일

```
src/Pipeline/Reconstruction/DepthCameraFrameSource.h     게이트 + 통계 + 백프로젝션
src/Pipeline/Reconstruction/RealSenseDepthProvider.h/.cpp  RealSenseOptions
src/TSDF/Backends/AdvancedTSDF.h/.cpp                    AdaptiveBandConfig + push constant
src/TSDF/Backends/kernel_AdvancedTSDF.integrate.comp.glsl σ_z 밴드
src/TSDF/Backends/TSDFBackend.h/.cpp                     경계 전용 TSDFAdaptiveBand
src/Pipeline/Types.h, Integration/IntegrationThread.cpp  MapConfig 배선
example2/realsense_scan.cpp, icp_quality_diag.cpp,
example2/tsdf_folder_eval.cpp                            CLI 플래그 + 카운터 출력
test/test_depthFrontend.cpp, test/test_advancedTsdf.cpp  테스트 20개
```

## 참고

- Nguyen, Izadi, Lovell, *Modeling Kinect Sensor Noise for Improved 3D Reconstruction and Tracking*, 3DIMPVT 2012 — 노이즈 모델과 KinectFusion 적용(필터·ICP 가중·TSDF 트런케이션)
- Sterzentsenko et al., *Self-Supervised Deep Depth Denoising*, ICCV 2019 — D415 사전학습 모델, https://github.com/VCL3D/DeepDepthDenoising
